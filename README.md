# CSF101-Algoritma_Pemrograman

You'll find how i solve CSF101 assignment here 📚 <br>
Feel free to look around 👀 <br>

<hr>

## How to: Push your files into 𑄝Github repository
```bash
git init
```
- Create a .git folder inside your workspace

```bash
git add remote (Give remote name, or just use origin) https://github.com/name/repositoryName
```
- Add remote, give it a name, and assign your repository link

```bash
git branch -m (Give branch name, or just use master or main)
```
- Create a branch, give it a name

```bash
git add .
```
- Add all your file into local branch
### OR
```bash
git add (File name)
```
- Add specific file into your local branch

```bash
git commit -m "Give commit message"
```
- Commit change's before pushing into repository

```bash
git push -u (Your remote name) (Your branch name)
```
- Push your files into your repository branch